import App from './app';
import './global.css';

export const metadata = {
  title: 'Welcome to next-web',
  description: 'Generated by create-nx-workspace',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body suppressHydrationWarning={true}>
        <App>{children}</App>
      </body>
    </html>
  );
}
