# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build nest-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nest-api`.
FROM node:18-alpine

ENV NODE_ENV production

USER node
WORKDIR /home/node

COPY ./dist/apps/nest-api/* ./
RUN npm ci

CMD ["node", "main.js"]
